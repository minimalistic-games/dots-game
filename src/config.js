const config = {
  colorCodes: {
    white: [255, 255, 255],
    red: [192, 64, 0],
    blue: [0, 96, 192],
  },

  cellSize: {
    min: 10,
    max: 100,
    default: 40,
  },

  firstPlayer: 'red',
};

export default config;
